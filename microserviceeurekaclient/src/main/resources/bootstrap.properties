#配置client往哪个注册中心去注册，一般会默认，但是显示地表示出来
eureka.client.service-url.defaultZone=http://localhost:8761/eureka

#client的名成
spring.application.name=myfirstclient

server.port=10086

#配置中心的信息，默认是8888端口
spring.cloud.config.uri=http://localhost:8088
#生产环境dev，测试test，对应github上的文件
spring.cloud.config.profile=dev